namespace MachineryManagerEnterprise.UI.Theming;

/// <summary>
/// The corner treatment applied to inputs, buttons, cards and other
/// surfaces across the whole application, independently of the chosen
/// <see cref="AppThemeMode"/>.
/// </summary>
public enum AppCornerStyle
{
    /// <summary>Square corners (zero border radius).</summary>
    Sharp,

    /// <summary>Rounded corners.</summary>
    Rounded
}